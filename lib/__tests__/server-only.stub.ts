/**
 * server-only stub — vitest 환경 alias용.
 *
 * Next.js 'server-only' 패키지는 client 번들에 포함되면 build 시 에러를 던지는 가드 모듈이다.
 * 본 stub은 vitest.config.ts의 resolve.alias로만 사용되며, 런타임 동작은 noop이다.
 */
export {};
